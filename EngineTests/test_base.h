#pragma once

#include "core/core.h"

#include "object/object.h"

using namespace engine;

namespace EngineTests
{
  // not possible until the dynamic rttibecause the type will have to be registered in the enum in the engine project
  
  //class ENGINE_API test_base : public object
  //{
  //public:
  //  OBJECT_DECLARE(test_base, object)
  //};
}
